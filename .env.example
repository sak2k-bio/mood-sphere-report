# This is an example environment file
# Copy this to .env.local and update with your values

# No environment variables are required for MoodSphere
# as it's a client-side only application

# Optional: If you want to add analytics in the future
# NEXT_PUBLIC_GA_ID=your-google-analytics-id

# Optional: If you want to add error tracking
# NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn